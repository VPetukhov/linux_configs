- name: Install and configure basic Ubuntu packages
  hosts: targets
  become: true

  tasks:
    - name: System packages
      ansible.builtin.package:
        name:
          - build-essential
          - zsh
          - vim
          - htop
          - git
          - micro
